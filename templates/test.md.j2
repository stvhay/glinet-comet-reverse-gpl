{# Proof-of-concept Jinja template demonstrating TrackedValue with automatic footnotes #}
{% set test = analyze('test') %}

# Template POC Test

This document is generated from a Jinja template to demonstrate the analysis framework with automatic source attribution.

## Test Data with Source Tracking

The following values use the `| src` filter to automatically add footnote references to their source scripts:

- **Test Value**: {{ test.test_value | src }}
- **Test Hex**: `{{ test.test_hex | src }}`
- **Test String**: {{ test.test_string | src }}
- **Discovered Offset**: {{ test.discovered_offset | src }}

## Regular Values (No Source Tracking)

This value has no source metadata, so it renders without a footnote:

- **Cache Test**: {{ test.cache_test }}

## Caching

If you run the renderer twice, the second run should use cached results from `results/test.toml`.

Check the manifest at `results/.manifest.toml` to see the hashes that track cache validity.

---

*Generated by Jinja template POC*

{{ render_footnotes() }}
